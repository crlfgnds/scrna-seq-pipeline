/*
 * Local profile — run on a workstation/laptop using Docker.
 * Activated by: -profile local
 */

docker.enabled = true

// modest resource caps so a single machine isn't overwhelmed
process {
    cpus   = 4
    memory = '16 GB'
}
